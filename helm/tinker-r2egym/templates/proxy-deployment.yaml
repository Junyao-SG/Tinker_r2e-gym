{{- if .Values.proxy.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tinker-r2egym.fullname" . }}-proxy
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tinker-r2egym.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "tinker-r2egym.proxy.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "tinker-r2egym.proxy.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      nodeSelector:
        role: system
      tolerations:
        - key: role
          value: system
          effect: NoSchedule
      containers:
        - name: proxy
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: Always
          command:
            - python
            - -m
            - tinker_r2egym.tinker_proxy
            - --model_name
            - {{ .Values.proxy.model.name | quote }}
            {{- if .Values.proxy.model.weightsPath }}
            - --weights_path
            - {{ .Values.proxy.model.weightsPath | quote }}
            {{- end }}
          ports:
            - containerPort: 8080
              name: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          env:
            - name: TINKER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: tinker-credentials
                  key: TINKER_API_KEY
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: tinker-credentials
                  key: HF_TOKEN
                  optional: true
          resources:
            requests:
              cpu: "1"
              memory: "2Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
{{- end }}
