apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "r2e-tinker.fullname" . }}-sandbox-deny-egress
  namespace: {{ .Values.sandbox.namespace }}
spec:
  podSelector:
    matchLabels:
      app: swe-sandbox
  policyTypes:
    - Egress
  egress: []
